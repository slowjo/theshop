import React from 'react';
import Layout from '../components/layout';

export default () => (
    <Layout>
        <div className="container py-2 not-found-container">
            <h1>Sorry, page not found.</h1>
        </div>
    </Layout>
);